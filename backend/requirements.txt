# Runtime dependency for the Python intelligence core.
# The suspicion score is a documented heuristic (stdlib re/math), so no
# xgboost/scikit-learn artifact is required. The bill correlation runs from a
# local map (no supabase client needed). Only the LLM summary needs a package,
# and it degrades gracefully when OPENAI_API_KEY is absent.
xgboost==2.0.3
scikit-learn==1.4.1.post1
openai==1.14.1
supabase==2.4.0
python-dotenv==1.0.1
yfinance==0.2.52
requests==2.32.3
