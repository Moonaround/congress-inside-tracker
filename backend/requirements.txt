# Python intelligence core dependencies (must match what pipeline.py imports).
openai==1.14.1          # LLM synthesis via OpenRouter's OpenAI-compatible gateway
httpx==0.27.2           # pin: openai 1.14.1 breaks on httpx>=0.28 ("proxies" TypeError)
python-dotenv==1.0.1    # load backend/.env
yfinance==0.2.52        # company / industry / price lookups
requests==2.32.3        # Alpha Vantage OVERVIEW (high-risk targets only)
chromadb==0.5.5         # local persistent vector store for bill correlation
